name: Package GEDKeeper3 for Fedora

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  DOTNET_VERSION: '6.0.*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Init
      uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: Debug
      run: rpmbuild --version
    - name: Debug
      run: dotnet sdk check
    - name: Build
      run: dotnet build -c Linux_Release
      working-directory: projects/GKv3
    - name: Debug
      run: du -d1 -h
    - name: Debug
      run: ls -la && gk_linux_rpm_fedora_package.sh
      working-directory: deploy
